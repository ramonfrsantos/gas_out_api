# Spring config
spring:
  application:
    name: gas-out-api
  datasource:
    url: jdbc:${DATABASE_URL}
    username: ${USER}
    password: ${PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update